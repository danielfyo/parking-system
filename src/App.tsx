import { Button } from 'antd';
import './App.css';
import { FC } from 'react';

const App: FC = () => (
  <div className="App">
    <Button type="primary">Get Users</Button>
  </div>
);

export default App;
